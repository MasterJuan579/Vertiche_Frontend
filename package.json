{
  "name": "vertiche-frontend",
  "private": true,
  "version": "2.0.0",
  "description": "Vertiche SortFlow — unified shell + per-module workspaces. Single Vercel deploy.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "preview": "npm run preview --workspace=web"
  }
}
